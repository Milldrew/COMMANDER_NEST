import { PersonInfoQuestions } from './person-info-questions';

describe('PersonInfoQuestions', () => {
  it('should be defined', () => {
    expect(new PersonInfoQuestions()).toBeDefined();
  });
});
